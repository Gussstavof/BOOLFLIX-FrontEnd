version: '3.9'
services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: flix-frontend:latest
    container_name: flix-frontend
    ports:
      - "4200:80"
    restart: unless-stopped
